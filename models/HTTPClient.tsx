export class HTTPClient {
    private SERVER_URL = 'localhost:8080';

    request(
        path: string,
        options : {
            method: 'POST'|'GET',
            headers: {},
            body: {}
        }
    ) {
       // fetch()
    }
}
